/* import React, { useState } from "react";
import styled from "styled-components";

function InfoSession({ key, weekday, date }) {
    const [weekDays, setWeekDays] = useState(weekday)
  return (
    <>
      <InfoDay>

       weekday={weekday.map()} 
      date={date}
      
      
      </InfoDay>
    </>
  );
}

export default InfoSession;

const InfoDay = styled.p`
  font-family: Roboto;
  font-size: 20px;
  font-weight: 400;
  text-align: left;
  color: #293845;
`;
 */